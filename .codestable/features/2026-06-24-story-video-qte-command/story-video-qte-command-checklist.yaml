feature: 2026-06-24-story-video-qte-command
created: 2026-06-24
steps:
  - action: "Command 协议与 schema：建立 qte command 常量、NodeKind.Qte、默认 schema 和 compiler 输出"
    exit_signal: "编译 QTE 节点得到 qte command，schema 参数和 success/fail outcome 正确"
    status: done
  - action: "Runtime 校验与并行契约：补 Parallel + PlayVideo + Wait -> qte 的运行时验收"
    exit_signal: "wait 到点后 frame 同时包含 video command 与 qte command，CompleteCommand(success/fail) 可推进"
    status: done
  - action: "StoryPlayback custom surface：让 StoryPlayerView 对 qte 请求 Custom surface，并保持 continue 隐藏、video surface 保留"
    exit_signal: "测试 channel 收到 Video 与 Custom request，缺少 CustomRoot 报配置错误"
    status: done
  - action: "默认 QTE handler / overlay：实现最小点击/Space 输入、倒计时、success/fail 完成和 stop/cancel 清理"
    exit_signal: "达到 requiredCount 完成 success，超时完成 fail，停止命令不推进 outcome"
    status: done
  - action: "Editor / seek guard：补 QTE 节点编译校验和带 QTE 的视频不写入 __videoSeekPolicy=transition"
    exit_signal: "非法 duration / requiredCount / outcome 目标给出定位错误；QTE 互动视频不显示 seek policy"
    status: done
  - action: "范围守护与构建：跑相关 build / tests / grep"
    exit_signal: "未出现 TimedChoice、EvaluateMediaTime、Story seek、InputModule 接入或 Runtime/Story UI 引用"
    status: done
checks:
  - item: "存在 NodeKind.Qte，参数为 inputActionId、durationSeconds、requiredCount、promptTextKey，端口为 success/fail"
    source: "验收场景 N1"
    status: passed
  - item: "编译单个 QTE 节点得到 StoryStepKind.Command，command name 为 qte，waitForCompletion=true，schema argument definitions typed"
    source: "验收场景 N2"
    status: passed
  - item: "duration <= 0 或 requiredCount < 1 时返回定位错误"
    source: "验收场景 N3"
    status: passed
  - item: "success/fail 缺目标或出现其它默认 outcome 时返回定位错误或注册失败"
    source: "验收场景 N4"
    status: passed
  - item: "Parallel(PlayVideo, Wait -> QTE) Evaluate 到点后 frame 同时包含 video command 和 qte command"
    source: "验收场景 N5"
    status: passed
  - item: "默认 QTE overlay 输入达到 requiredCount 后完成 success 并推进到 success target"
    source: "验收场景 N6"
    status: passed
  - item: "duration 到期未达到 requiredCount 后完成 fail 并推进到 fail target"
    source: "验收场景 N7"
    status: passed
  - item: "wait 到点后的 frame 让 channel 收到 Custom request；同帧视频也收到 Video request；continue button 不显示"
    source: "验收场景 N8"
    status: passed
  - item: "channel 对 QTE custom request 返回 null root 时抛配置错误"
    source: "验收场景 N9"
    status: passed
  - item: "QTE command 离开 frame 或播放停止时 overlay 被清理，handle stop/cancel 不触发 success/fail"
    source: "验收场景 N10"
    status: passed
  - item: "QTE 与 video 同帧出现时 video command handle 保持活跃，不调用 pause/seek"
    source: "验收场景 N11"
    status: passed
  - item: "包含 QTE 的互动视频编译产物中 play_video 不包含 __videoSeekPolicy=transition"
    source: "验收场景 N12"
    status: passed
  - item: "不新增 StoryStepKind.Qte、TimedChoice、EvaluateMediaTime、StoryRunner.Seek 或 StoryModule.Seek"
    source: "范围守护"
    status: passed
  - item: "默认 QTE 不新增 timeout/canceled outcome，不接入 InputModule 或 Unity Input System action asset"
    source: "范围守护"
    status: passed
  - item: "Runtime/Story 不引用 UGUI、AVPro、UIWindow、Editor graph 或播放窗口类型"
    source: "Runtime 隔离"
    status: passed
