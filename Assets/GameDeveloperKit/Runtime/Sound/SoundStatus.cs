namespace GameDeveloperKit.Sound
{
    /// <summary>
    /// 定义 Sound Status 枚举。
    /// </summary>
    public enum SoundStatus
    {
        None,
        Loading,
        Playing,
        Paused,
        Stopped,
        Completed,
        Failed,
        Released,
    }
}
